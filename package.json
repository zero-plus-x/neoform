{
  "private": true,
  "description": "Higher-Order Components for form state management and validation in React",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "start-babel-cli": "^4.0.2",
    "start-deepsweet-react-components-monorepo-preset": "^0.0.9"
  },
  "scripts": {
    "start": "start-runner --preset start-deepsweet-react-components-monorepo-preset"
  }
}
